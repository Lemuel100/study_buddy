import 'package:flutter/material.dart';

class CorseGuid extends StatelessWidget {
  const CorseGuid({super.key});

  static String routeName = '/corseGuide';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
